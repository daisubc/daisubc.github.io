# URL of the backend deployed on Render (no trailing slash).
# For local dev this is http://localhost:8000
VITE_API_BASE=http://localhost:8000

# Google OAuth client ID (Web application) with the Gmail API enabled.
# Leave blank to use demo mode only.
VITE_GOOGLE_CLIENT_ID=
